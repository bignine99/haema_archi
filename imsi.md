# 📝 [다음 작업 대기열] Phase B: 층별 조닝 & 스페이스 모듈 (Space Programming) 연속 작업

> 업데이트: 2026-03-29 저녁  
> **현재 세션의 UI 개편(3-Tab 레이아웃, 수동 면적 수정, 차트 6종)과 AI 프롬프트 수정(95~100% 한계 강제)은 선행 완료되었습니다. 이어서 진행할 작업 내역입니다.**

---

## 🎯 다음 세션 주요 목표
사용자가 더욱 유연하게 공간 프로그래밍을 검토하고, 이를 최종적으로 3D 매스 솔루션(Phase C)에 완벽 파이프라인으로 연결하는 작업.

---

### 🚀 1. 층/존(Zone)/실(Room) 단위 편집 기능 완전화
- [ ] **실(Room) 직접 추가/삭제 기능:** 현재 층(Floor)에 대해서는 삭제/추가가 가능하나, 개별 실 단위의 Add/Delete 버튼 추가 필요.
- [ ] **Drag & Drop 조닝 세밀화:** 현재 구현된 Drag & Drop 기능 안정화 (특정 영역 드롭 시 하이라이트 표시 및 인덱스 정렬 버그 방지).
- [ ] **Zone명 변경 기능:** "소계(XXX)" 등으로 표기된 Zone과 Primary Use의 이름을 사용자가 직접 Input으로 바꿀 수 있도록 개선.

### 🎨 2. 데이터 위반 실시간 피드백 강화
- [ ] **인풋 필드(Input) 실시간 경고:** 하단 배분 현황 막대뿐만 아니라, `updateRoomArea`를 통해 입력 시 특정 마지노선을 넘어가면 해당 텍스트 인풋 박스의 테두리를 붉은색(`border-red-500`)으로 하이라이터하여 오류 지점을 직관적으로 안내.
- [ ] **총 사업비 실시간 계산기 연동:** 면적 변경 시마다, 입력된 평당 단가(공사비)를 바탕으로 총 공사비(Total Construction Cost)가 실시간으로 변하는 지표를 대시보드 어딘가에 작게 추가.

### 🧩 3. Phase C 실시간 매스 연동 결속력 점검 (MSA 파이프라인)
- [ ] 현재의 상태(`floorZoning` 등) 데이터 트리 포맷이, 우측 상단 `Phase C 실시간 매스 연동` 버튼 클릭 시 `optimization-engine` 또는 매스 제너레이터로 누락 없이 전송되는지 네트워크 로드맵 점검 필요.
- [ ] 각 Zone의 형태를 추정할 수 있는 기본 메타데이터(예: Zone별 필요 최소 층고 등) 매핑 확인.

### 📊 4. 데이터 내보내기 (Export)
- [ ] 현재 시각화된 "층별 세부용도 및 면적표"를 Excel(또는 CSV) 파일 구조로 다운로드할 수 있는 내보내기 버튼 추가. (SheetJS 도입 또는 Blob 다운로드 등)
- [ ] 통계 차트 패널 화면을 요약 보고서 용도로 캡처 제공.

---
**기타 참고:** 로컬 저장 로직(Zustand Persist)이 잘 작동하고 있는지 테스트 후, 브라우저 이탈 시 작성했던 데이터가 완전히 손실되지 않도록 Auto-Save 기능 구현 시기를 조율할 것!
